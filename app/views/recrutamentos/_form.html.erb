<%= form_with(model: recrutamento, local: true) do |form| %>
  <% if recrutamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recrutamento.errors.count, "error") %> prohibited this recrutamento from being saved:</h2>

      <ul>
      <% recrutamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
