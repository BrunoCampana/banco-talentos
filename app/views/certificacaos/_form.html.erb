<%= form_with(model: certificacao, local: true) do |form| %>
  <% if certificacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(certificacao.errors.count, "error") %> prohibited this certificacao from being saved:</h2>

      <ul>
      <% certificacao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
