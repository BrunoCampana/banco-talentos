<%= form_with(model: cargoocupado, local: true) do |form| %>
  <% if cargoocupado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cargoocupado.errors.count, "error") %> prohibited this cargoocupado from being saved:</h2>

      <ul>
      <% cargoocupado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
